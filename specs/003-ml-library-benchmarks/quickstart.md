# Quickstart: ML Library Benchmarks

**Date**: 2025-10-16

## Prerequisites

- Features 001 & 002 complete
- Python 3.10+ with pip
- Install: `pip install tensorflow-macos torch transformers`

## Usage

### P1: Run framework benchmark locally
```bash
macmlbench menu
# Select framework benchmark
# Choose model size (small/medium/large)
```

### P2: Deploy models to remote machines
```bash
# Models auto-deploy when running multi-machine framework benchmarks
# Progress shown during transfer
```

### P3: LLM inference across machines
```bash
# Select LLM benchmark
# See loading → prefill → decode progress per machine
```

Phase 1 complete - ready for implementation.
